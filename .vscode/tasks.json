{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build cpp_bin",
      "type": "shell",
      "command": "cmake -S ${workspaceFolder}/cpp -B ${workspaceFolder}/cpp/build -DCMAKE_BUILD_TYPE=Debug && cmake --build ${workspaceFolder}/cpp/build",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": "$gcc"
    }
  ]
}
